import { MvcPartialDirective } from './mvc-partial.directive';

describe('MvcPartialDirective', () => {
  it('should create an instance', () => {
    const directive = new MvcPartialDirective();
    expect(directive).toBeTruthy();
  });
});
