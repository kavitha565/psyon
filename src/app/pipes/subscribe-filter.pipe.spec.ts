import { SubscribeFilterPipe } from './subscribe-filter.pipe';

describe('SubscribeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscribeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
